= simple_form_for(epic? ? [story.epic, story] : story) do |f|
  %fieldset
    %legend Content
    = f.input :title
    = f.input :body
  %fieldset
    %legend Other
    = f.association :epic, label_method: :title
  .actions
    = f.submit class: 'button radius'
